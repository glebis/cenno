{
  "$description": "Example ~/.cenno/tokens.json — overrides built-in cenno design tokens (W3C DTCG). Drop only the tokens you want to change; the rest keep their built-in values. Flattened to --cenno-* CSS variables at runtime.",
  "color": {
    "$type": "color",
    "flow": {
      "mood": { "$value": "#E85D9C" },
      "question": { "$value": "#6C4DF6" },
      "ema": { "$value": "#0EA5A5" }
    }
  }
}
